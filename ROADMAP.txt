╔══════════════════════════════════════════════════════════════════════════╗
║           DISSECT — YOL HARİTASI v5  (ÜST SEVİYE)                     ║
║           AI-Powered Binary Analysis & Reverse Engineering Studio      ║
║           Başlangıç: 2026-04-07 · Güncelleme: 2026-04-09              ║
║                                                                        ║
║           Vizyon: IDA Pro + x64dbg + Wireshark + Local AI              ║
║           Tek masaüstü uygulamasında birleşik RE platformu             ║
╚══════════════════════════════════════════════════════════════════════════╝

  Durum:  [ ] = yapılmadı   [~] = kısmen   [x] = tamamlandı
  Risk:   ★ = düşük risk    ★★★ = yüksek risk

══════════════════════════════════════════════════════════════════════════
  MEVCUT DURUM ÖZETİ
══════════════════════════════════════════════════════════════════════════

  Çekirdek Mimari (Tauri 2 + React 18 + Rust)      ✅ Tamamlandı
  ─────────────────────────────────────────────────
  • 12 modüler sayfa (code-split + lazy load)
  • Zustand state yönetimi + localStorage persistence
  • 40+ Rust Tauri komutu (PE parsing, disasm, hashing, debugger)
  • Web Worker offloading (entropy, strings, CRC32, MD5)
  • Virtual scroll (react-window v2) + Vite bundle optimizasyonu

  Çalışan Özellikler                                🟢
  ─────────────────────────────────────────────────
  • PE Scanner (goblin + capstone): full string/import/section/entropy/packer/risk analizi
  • Hex Patcher: byte patching, checksum recalc, pattern search, export/import
  • AI Chat: Ollama + LM Studio + GGUF Direct, 6 mod, streaming, RAG context
  • Disassembly: x86/x64 instruction listing, function finder, CFG (React Flow)
  • System & Models: GPU/CUDA detection, HuggingFace search, model download
  • Hashing: MD5/SHA1/SHA256/CRC32/imphash/fuzzy
  • Plugin Framework: sandboxed JS loader + örnek pluginler
  • Dashboard: tarama geçmişi, istatistikler, proje yönetimi

  Kısmi / Mock Durumda                             🟡
  ─────────────────────────────────────────────────
  • Process Attach: enumeration çalışıyor, bellek okuma test edilmeli
  • Debugger: Rust komutları var, UI mock register/breakpoint gösteriyor
  • Emulation: Unicorn backend var, UI iskelet seviyesinde
  • Network Capture: mock paketler, gerçek capture kısmen bağlı
  • FLIRT Signatures: statik pattern veritabanı, gerçek matching yok


══════════════════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════════════════
       Y  E  N  İ     Y  O  L     H  A  R  İ  T  A  S  I
══════════════════════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════════════════════

  SOL PANEL MİMARİSİ (NavSection — Açılır/Kapanır Gruplar)
  ─────────────────────────────────────────────────────────
  Sol panel 3 ana gruba bölündü (NavSection component):

     ▾ ANALYSIS          ← varsayılan açık
       • Collector Layer
       • Hex Patcher
       • Disassembly

     ▾ INTELLIGENCE       ← varsayılan açık
       • AI Chat
       • System & Models
       • Plugins
       • Dashboard

     ▸ ADVANCED           ← varsayılan kapalı (aktif sayfa varsa açılır)
       • Process Attach
       • Debugger
       • Emulation
       • Net Capture
       • FLIRT Sigs

  Yeni özellikler eklenirken uygun gruba NavItem olarak eklenir.
  Grup çok büyürse yeni NavSection açılır. Her section bağımsız
  açılır/kapanır, aktif sayfa varsa otomatik açılır + mavi nokta.


══════════════════════════════════════════════════════════════════════════
  FAZ A — STABİLİTE & GERÇEK ENTEGRASYON         Öncelik: ★★★★★
══════════════════════════════════════════════════════════════════════════

  Hedef: Mock/stub sayfaları gerçek backend'lerle bağla.
         Mevcut özellikleri production-grade yap.
  Sidebar: Bu faz mevcut Advanced grubundaki sayfalara bağlanır,
           yeni sayfa eklenmez.

  A1 — Debugger Gerçek Entegrasyonu
  ──────────────────────────────────
  [x] Rust attach_debugger → UI'ya gerçek register feed'i bağla
  [x] Breakpoint set/remove → gerçek INT3 injection
  [x] Step Into / Step Over / Continue → gerçek debugger kontrolü
  [x] Call stack → gerçek stack frame parsing
  [x] Watch expressions (memory/register izleme)
  [x] Debug event stream (exception, module load, thread create)

  A2 — Process Attach & Memory Inspection
  ─────────────────────────────────────────
  [x] Process memory map → gerçek VirtualQueryEx sonuçları doğrula
  [x] Hex dump → canlı bellek hex viewer (read_process_memory)
  [x] Module list (DLL'ler) → güvenilir enumeration
  [x] Thread list + suspend/resume
  [x] Memory search (byte pattern / string arama canlı bellekte)

  A3 — Emulation Engine UI Bağlantısı
  ────────────────────────────────────
  [x] Unicorn emulate_function → UI trace viewer'a bağla
  [x] Register değişim animasyonu gerçek veriye bağlansın
  [x] Memory write log → gerçek push/call mem write takibi
  [x] Breakpoint/watchpoint desteği emülasyonda
  [x] Emülasyon hız kontrolü (step / run / pause)

  A4 — Network Capture Gerçek Veri
  ─────────────────────────────────
  [x] get_process_connections → polling ile canlı bağlantı izleme
  [x] DNS query logging (port 53 bağlantıları)
  [x] TCP stream reconstruction (src-dst grubu)
  [x] Connection timeline görselleştirme
  [x] Export: JSON

  A5 — FLIRT Signature Matching
  ──────────────────────────────
  [x] Import tablosunu FLIRT pattern DB ile eşleştir
  [x] Tanınan kütüphane fonksiyonlarını disassembly'de göster
  [x] Kullanıcı imza ekleme/düzenleme
  [x] FLIRT .sig dosyası import desteği (gelecek sprint)


══════════════════════════════════════════════════════════════════════════
  FAZ B — İLERİ STATİK ANALİZ                    Öncelik: ★★★★☆
══════════════════════════════════════════════════════════════════════════

  Hedef: Profesyonel RE araçlarına yakın statik analiz derinliği.
  Sidebar: Decompiler → Disassembly sayfasının alt-tab'ı olarak entegre.
           BinDiff → Analysis grubuna yeni sayfa (gerekirse).
           Diğerleri mevcut sayfalara panel olarak eklenir.

  B1 — Decompiler (Pseudo-C)
  ───────────────────────────
  [x] Disassembly → SSA (Static Single Assignment) dönüşümü
  [x] Control flow recovery (if/else, while, for, switch)
  [x] Variable naming heuristics (stack offset → "local_xx")
  [x] AI-assisted variable/function renaming
  [x] Decompile çıktısı ↔ disassembly senkronizasyonu (Analysis panel Pseudo-C tab)
  Risk: ★★★ (çok zor, incremental yaklaş)

  B2 — Type Recovery & Struct Editor
  ────────────────────────────────────
  [x] Stack frame analizi → struct inference (recover_types backend)
  [x] vtable parsing → C++ class hierarchy
  [x] RTTI bilgisinden sınıf isimleri
  [x] Kullanıcı tanımlı struct/enum type'lar
  [x] Type propagation (Types tab — Disassembly Analysis panel)

  B3 — Advanced String & Data Analizi
  ─────────────────────────────────────
  [x] Encrypted/encoded string tespiti (XOR, Base64, RC4 loop)
  [x] Otomatik string decryption (emülasyon ile)
  [x] Kaynak (resource) görselleştirme (icon, dialog, version info)
  [x] Certificate / Authenticode analizi

  B4 — Cross-Binary Analiz (BinDiff)
  ────────────────────────────────────
  [x] İki PE'nin fonksiyon listesini eşleştir (isim + hash)
  [x] Değişen / eklenen / silinen fonksiyonları vurgula
  [x] Inline diff (instruction seviyesinde)
  [x] Patch vs original otomatik karşılaştırma
  [x] HTML diff rapor export


══════════════════════════════════════════════════════════════════════════
  FAZ C — DİNAMİK ANALİZ & SANDBOX               Öncelik: ★★★★☆
══════════════════════════════════════════════════════════════════════════

  Hedef: Çalıştırılan binary'nin davranışını izle ve raporla.
  Sidebar: API Tracing + Sandbox → Advanced grubuna eklenir.
           Taint → Disassembly/Debugger sayfasına entegre.
           Anti-Analysis → mevcut sayfalara panel olarak.

  C1 — API Call Tracing
  ──────────────────────
  [x] IAT hooking veya Detours ile API call logging
  [x] Kategori: File, Registry, Network, Process, Memory
  [x] Her API çağrısı: parametre + return value
  [x] Timeline + filtreleme UI
  [x] Suspicious API pattern detection (injection, persistence)

  C2 — Sandbox Execution
  ────────────────────────
  [x] İzole ortamda binary çalıştır (restricted token / AppContainer)
  [x] Dosya sistemi değişiklik izleme (minifilter / USN journal)
  [x] Registry değişiklik izleme
  [x] Ağ trafiği → Network Capture entegrasyonu
  [x] Otomatik davranış raporu (AI destekli)
  Risk: ★★★ (güvenlik kritik)

  C3 — Taint Analysis
  ─────────────────────
  [x] Kullanıcı girdisini (argv, stdin, recv) tag'le (taint_analysis backend)
  [x] Veri akışını register/memory üzerinden takip et
  [x] "User input → dangerous API" path tespiti
  [x] UI'da tainted data vurgulama (Taint tab — Disassembly Analysis panel)

  C4 — Anti-Analysis Bypass
  ──────────────────────────
  [x] Anti-debug teknik tespiti ve bypass (IsDebuggerPresent, NtQueryInformationProcess)
  [x] Anti-VM tespiti ve bypass (CPUID, registry, MAC)
  [x] Timing check tespiti ve patch önerisi
  [x] Packer detection → otomatik unpack pipeline


══════════════════════════════════════════════════════════════════════════
  FAZ D — AI v2: PROAKTIF ASISTAN                 Öncelik: ★★★★☆
══════════════════════════════════════════════════════════════════════════

  Hedef: AI'ı reaktif chatbot'tan akıllı analiz ortağına dönüştür.
  Sidebar: Yeni sayfa eklenmez. Tüm AI özellikleri mevcut
           AI Chat sayfasının sekmeleri/modları olarak entegre.

  D1 — AI Agent Pipeline
  ────────────────────────
  [x] ReAct pattern: AI kendi başına scan → disasm → analiz → rapor
  [x] Tool-use: AI, backend komutlarını doğrudan çağırabilsin (ai_agent_task)
  [x] Multi-step reasoning: "Bu binary neden tehlikeli?" → adım adım
  [x] Kullanıcı onayı ile action execution (ChatPage Agent modu)

  D2 — RAG & Knowledge Base
  ──────────────────────────
  [x] Tarama geçmişini SQLite vector DB'ye indexle
  [x] "Benzer binary daha önce taradım mı?" → semantic search
  [x] MITRE ATT&CK, CVE, malware family tanımları embed
  [x] AI yanıtlarında kaynak referansı göster

  D3 — Otomatik Rapor Üretimi
  ─────────────────────────────
  [x] Executive summary + teknik detay + IOC listesi
  [x] PDF export (jsPDF veya Rust wkhtmltopdf)
  [x] Dil seçimi: TR / EN / custom template
  [x] Rapor geçmişi + karşılaştırma

  D4 — Cloud AI Fallback
  ────────────────────────
  [x] OpenAI / Anthropic / Groq API desteği
  [x] API key güvenli saklama (tmp dizin, üretimde keyring)
  [x] Local model yoksa → cloud fallback
  [x] Token kullanımı + maliyet takibi


══════════════════════════════════════════════════════════════════════════
  FAZ E — PLATFORM & EKOSİSTEM                    Öncelik: ★★★☆☆
══════════════════════════════════════════════════════════════════════════

  Hedef: Cross-platform, otomasyon, topluluk.
  Sidebar: Scripting → Intelligence grubuna yeni sayfa.
           Collaboration → Intelligence grubuna yeni sayfa.
           Diğerleri backend/CLI, sidebar etkilemez.

  E1 — Multi-Format Destek
  ──────────────────────────
  [x] ELF binary analizi (Linux) → full feature parity
  [x] Mach-O analizi (macOS)
  [x] Raw shellcode / firmware blob yükleme
  [x] .NET assembly (CIL disassembly)
  [x] APK / DEX (Android) temel analiz

  E2 — CLI & Otomasyon
  ──────────────────────
  [x] dissect-cli: komut satırından tarama + rapor
  [x] JSON çıktı (pipe-friendly, jq uyumlu)
  [x] CI/CD entegrasyonu: build pipeline'da binary scan
  [x] Batch scan: klasör tarama + toplam rapor
  [x] REST API modu (headless server)

  E3 — Scripting Engine
  ──────────────────────
  [x] IDAPython benzeri JS/Lua scripting desteği
  [x] Monaco Editor entegrasyonu (syntax highlight + autocomplete)
  [x] Script kütüphanesi + paylaşım
  [x] Otomatik analiz scriptleri (recipe sistemi)

  E4 — Plugin Ekosistemi v2
  ──────────────────────────
  [x] Remote plugin repository (topluluk mağazası)
  [x] Plugin API v2: disassembly hook, scanner hook, UI extension
  [x] Plugin güvenlik sandbox'ı güçlendir
  [x] Plugin dependency management

  E5 — Collaboration & Sharing
  ─────────────────────────────
  [x] Tarama sonucu paylaşım (encrypted link)
  [x] Takım workspace (çoklu kullanıcı, yorum, atama)
  [x] WebSocket ile real-time cursor + annotation sync
  [x] Audit log: kim ne zaman ne taradı


══════════════════════════════════════════════════════════════════════════
  FAZ F — UI/UX & POLISH                          Öncelik: ★★★☆☆
══════════════════════════════════════════════════════════════════════════

  Hedef: Profesyonel, hızlı, erişilebilir arayüz.
  Sidebar: Hex Editor Pro → Analysis grubundaki Hex Patcher'ı genişletir.
           Yeni sayfa eklenmez, mevcut yapı güçlendirilir.

  F1 — Panel & Layout Sistemi
  ─────────────────────────────
  [x] Dock panel sistemi (split view, tab, float, drag-drop)
  [x] Layout kaydetme / yükleme (workspace presets)
  [x] Çoklu monitör desteği (panel pop-out)
  [x] Keyboard shortcuts (IDA/x64dbg uyumlu mapping)

  F2 — Hex Editor Pro
  ─────────────────────
  [x] Tam hex editor (dosya üzerinde in-place edit)
  [x] Data inspector (selection → int8/16/32/64, float, string)
  [x] Bookmark sistemi (offset'lere isim ver)
  [x] Binary template (struct overlay görselleştirme)

  F3 — Gelişmiş Tema & i18n
  ───────────────────────────
  [x] Tema editörü (kullanıcı renk picker ile tema oluştursun)
  [x] İngilizce + diğer dil desteği genişlet
  [x] Font size / family ayarları
  [x] Erişilebilirlik: screen reader, high contrast

  F4 — Performans & Büyük Dosya Desteği
  ───────────────────────────────────────
  [x] 100MB+ PE dosya desteği (streaming parse)
  [x] Disassembly: 1M+ instruction virtual scroll akıcılığı
  [x] Lazy section loading (sadece görünen bölümü oku)
  [x] Bellek profiling + sızıntı tespiti


══════════════════════════════════════════════════════════════════════════
  ÖNERİLEN GELİŞTİRME SIRASI
══════════════════════════════════════════════════════════════════════════

  Sprint 1 → FAZ A (Stabilite & Entegrasyon)
  ─────────────────────────────────────────────
  Mock sayfaları gerçek veriye bağla: Debugger, Process
  Attach, Emulation, Network Capture, FLIRT.
  En kritik faz — mevcut altyapı zaten var.

  Sprint 2 → FAZ D (AI v2)
  ─────────────────────────────────────────────
  AI Agent pipeline, RAG, otomatik rapor.
  Kullanıcıya en görünür değer katacak faz.

  Sprint 3 → FAZ B (İleri Statik Analiz)
  ─────────────────────────────────────────────
  Decompiler, type recovery, cross-binary diff.
  RE aracının gerçek derinliğini belirleyecek faz.

  Sprint 4 → FAZ C (Dinamik Analiz)
  ─────────────────────────────────────────────
  API tracing, sandbox, taint analysis.
  Tam dinamik analiz yeteneği.

  Sprint 5 → FAZ F (UI/UX Polish)
  ─────────────────────────────────────────────
  Dock panel, hex editor pro, tema editörü.
  Profesyonel görünüm ve kullanım deneyimi.

  Sprint 6 → FAZ E (Platform & Ekosistem)
  ─────────────────────────────────────────────
  Multi-format, CLI, scripting, collaboration.
  Topluluk ve otomasyon odaklı büyüme.
  → 12.1  Cross-platform
  → 12.2  CLI modu
  → 12.3  Scripting engine
  → 10.3  Cloud IOC feed


══════════════════════════════════════════════════════════════════════════
  TEKNİK NOTLAR
══════════════════════════════════════════════════════════════════════════

  • Kod Bölme:  Vite dynamic import + React.lazy ile. Her Page
    component'ı kendi dosyasında. Shared state → Context veya Zustand.
    Migrasyon sırası: en bağımsız sayfadan başla (Dashboard → System
    → Plugins → Patcher → Chat → Scanner → Disasm → App shell).

  • windows-rs:  Process enumeration + debugger için windows crate.
    Cargo.toml'a features: ["Win32_System_Diagnostics_Debug",
    "Win32_System_Threading", "Win32_System_ProcessStatus",
    "Win32_Security"]. Admin yetki: manifest'e requestedExecutionLevel.

  • Unicorn Engine:  unicorn-engine Rust binding mevcut. x86/x64 arch,
    hook_add ile instruction/memory callback. Emulation → JSON state
    snapshot dizisi olarak frontend'e gönder.

  • RAG:  sqlite-vss (SQLite vector search) veya tantivy (Rust FTS).
    Her tarama sonucunu embedding'e dönüştür (local model ile).
    Soru geldiğinde → benzer taramaları bul → context'e ekle.

  • AI Agent:  Tauri komutlarını sırasıyla çağıran bir loop. Her
    adımda AI'a "şimdiye kadarki bulgularım" + "sıradaki hamle ne?"
    sor. ReAct format: Thought → Action → Observation → repeat.

  • Cross-platform:  Tauri 2 zaten Linux/macOS destekler. Platform-
    specific kodlar (windows-rs) → cfg(target_os) ile koşullu compile.
    Linux'ta ptrace, macOS'ta mach_* syscalls.

  • CLI:  clap crate + ayrı bin target (src/bin/dissect_cli.rs).
    JSON output → stdout. Non-interactive batch mode.


══════════════════════════════════════════════════════════════════════════
  MEVCUT DURUM ÖZETİ (v4 başlangıç)
══════════════════════════════════════════════════════════════════════════

  Tamamlanan:
    FAZ 1 — Disassembly & Kod Gezgini          (5/5 madde) ✅
    FAZ 2 — AI Gelişmiş Zeka                   (7/7 madde) ✅
    FAZ 3 — Rust Backend Güçlendirme           (6/6 madde) ✅
    FAZ 4 — Pro UX & Raporlama                 (6/6 madde) ✅
    FAZ 5 — Plugin Ekosistemi                  (4/4 madde) ✅
    FAZ 6 — İleri Seviye                       (5/5 madde) ✅
    ─────────────────────────────────────────────────
    TOPLAM: 33/33 madde · 12 view · 15 Rust komutu

  Teknik Borç:
    ⚠ App.jsx = 7163 satır (monolith → bölünmeli)
    ⚠ Bundle = 769 KB (hedef: <400 KB)
    ⚠ FAZ 6 sayfaları mock data kullanıyor (gerçek API gerekli)
    ⚠ localStorage dağınık (merkezi store gerekli)

  Hedef:
    FAZ 7-12 → 33 yeni madde · ~6 sprint · endüstriyel kalite

